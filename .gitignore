# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Build / deps
node_modules/
dist/
*.tsbuildinfo

# Misc
*.log
.DS_Store

# Internal session handoffs — kept locally for continuity, not published
plans/

# Owner-local live-test docs — contain real account data, never publish
kiwoom-mcp-test-log.md
kiwoom-mcp-test-prompts.md
